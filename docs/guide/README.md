## 安装

```bash
pnpm install
```

## 运行

```bash
pnpm start
```

也可以直接运行：

```bash
node index.js
```

启动后访问 `http://localhost:3000/`。默认账号为 `admin`，默认密码为 `Qqqaaa000`。

## 功能模块

- 我的基金：`/portfolio`
- 基金趋势：`/portfolio/history`
- 系统设置：`/settings`
